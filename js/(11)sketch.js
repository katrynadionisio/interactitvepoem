function setup() {
  createCanvas(windowWidth, windowHeight);
  textAlign(CENTER);
  textSize(30);
  colorMode(HSL);
}

function draw() {
  background('orange');
  text ('And gave it up.', windowWidth/2, windowHeight/2);
  fill (255);
  
  
    if (mouseIsPressed) {
      textSize (150);
    } else {
      
  
    }
    
    }
    